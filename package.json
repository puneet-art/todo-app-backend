{
  "name": "todo-app-backend",
  "version": "1.0.0",
  "description": "Backend for Todo App",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "build": "npx prisma generate && npx prisma db push",
    "dev": "nodemon src/server.js",
    "migrate:deploy": "npx prisma migrate deploy",
    "setup:dev": "npm install && npx prisma migrate deploy && nodemon src/server.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "axios": "^1.13.2",
    "bcryptjs": "^3.0.3",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.22.1",
    "jsonwebtoken": "^9.0.3",
    "prisma": "^5.22.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
